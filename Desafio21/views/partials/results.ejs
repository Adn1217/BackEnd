<% if (!("error" in colors)) {%>
    <table class="table">
        <thead>
            <th>Id</th>
            <th>Color</th>
        </thead>
        <tbody>
        <% for(let i=0; i<colors.length; i++){ %>
            <tr style="color:<%=colors[i]?.color%>">
                <td><%=colors[i]?.id%></td>
                <td><%=colors[i]?.color%></td>
            </tr>
        <% } %>
        </tbody>
    </table>
<% } %>